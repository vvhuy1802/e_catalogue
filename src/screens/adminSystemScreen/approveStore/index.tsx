import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const ApproveStore = () => {
  return (
    <View>
      <Text>ApproveStore</Text>
    </View>
  );
};

export default ApproveStore;

const styles = StyleSheet.create({});
